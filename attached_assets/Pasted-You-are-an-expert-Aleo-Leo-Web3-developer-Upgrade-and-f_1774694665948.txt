You are an expert Aleo + Leo + Web3 developer. Upgrade and finalize my existing project "PrivateClaw" to make it fully pass Aleo Buildathon judging.

DO NOT create a new project. Modify the existing repo.

---

## 🎯 GOALS

Fix and strengthen:

1. Non-trivial Leo contract logic
2. Frontend integration with contract
3. Deployment proof (without CLI)
4. README clarity for judges

---

## 🔥 TASK 1 — FINAL LEO CONTRACT (NON-TRIVIAL)

Replace /leo/src/main.leo with:

program private_claw.aleo {

```
// Validate bet input
transition validate_bet(amount: u64) -> bool {
    assert(amount > 0u64);
    assert(amount < 1000000u64);
    return true;
}

// Main betting logic
transition place_bet(amount: u64) -> u64 {
    assert(amount > 0u64);
    assert(amount < 1000000u64);

    let result: u64 = amount * 2u64;
    return result;
}

// Reward calculation logic
transition calculate_reward(amount: u64) -> u64 {
    assert(amount > 10u64);

    let reward: u64 = amount + 5u64;
    return reward;
}
```

}

---

## 🔥 TASK 2 — program.json

Ensure /leo/program.json contains:

{
"program": "private_claw.aleo",
"version": "0.0.1",
"description": "PrivateClaw privacy smart contract using Aleo",
"license": "MIT"
}

---

## 🔥 TASK 3 — FRONTEND INTEGRATION

Ensure frontend includes:

const PROGRAM_ID = "private_claw.aleo";

Add UI:

* Input field for amount
* Buttons:

  * "Validate Bet"
  * "Place Bet"
  * "Calculate Reward"

Each button should:

* Call corresponding simulation function
* Display result in UI

Example outputs:

validate_bet(50000u64) → true
place_bet(50000u64) → 100000u64
calculate_reward(50000u64) → 50005u64

---

## 🔥 TASK 4 — WALLET INTEGRATION

Ensure:

* "Connect Wallet" button exists
* Uses Shield Wallet
* Transaction buttons simulate or prepare calls to Leo contract

---

## 🔥 TASK 5 — DEPLOYMENT PROOF (CRITICAL)

Add to README:

## Aleo Deployment Proof

Program Name: private_claw.aleo
Network: Aleo Testnet

This project includes a fully functional Leo smart contract with multiple transitions:

* validate_bet
* place_bet
* calculate_reward

Due to environment limitations, deployment is simulated through Shield Wallet integration and frontend transaction construction.

All contract logic is written in Leo and structured for Aleo Testnet deployment.

---

## 🔥 TASK 6 — PRIVACY EXPLANATION (IMPORTANT FOR SCORING)

Add:

## Why This Uses Aleo

PrivateClaw leverages Aleo’s zero-knowledge execution model to simulate private betting logic.

All computations (validation, betting, and reward calculation) are designed to run privately via Leo smart contracts, ensuring that user inputs and financial data are not publicly exposed.

---

## 🔥 TASK 7 — HOW IT WORKS

Add:

## How It Works

1. User connects Shield Wallet
2. User enters bet amount
3. User clicks action button
4. Frontend calls Leo contract logic
5. Logic executes privately (simulated)
6. Result is displayed

---

## 🔥 TASK 8 — FINAL STRUCTURE

Ensure:

/leo
/src/main.leo
program.json

/frontend (existing)
README.md

---

## ⚠️ RULES

* DO NOT use Aleo CLI
* DO NOT remove UI
* DO NOT simplify logic
* MUST look like a real privacy application

---

## OUTPUT

Show:

1. Final Leo contract
2. Frontend changes
3. Final README
4. Project structure

Make it clean, realistic, and judge-ready.
