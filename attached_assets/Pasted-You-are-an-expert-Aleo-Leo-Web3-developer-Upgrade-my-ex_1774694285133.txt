You are an expert Aleo + Leo + Web3 developer. Upgrade my existing project "PrivateClaw" to make it pass Aleo Buildathon judging by improving:

1. Non-trivial Leo logic
2. Deployment proof (without using CLI)

DO NOT create a new project. Modify the existing repo.

---

## 🔥 TASK 1 — UPGRADE LEO CONTRACT (MAKE IT NON-TRIVIAL)

Replace current contract with a more advanced version:

program private_claw.aleo {

```
// Validates bet amount
transition validate_bet(amount: u64) -> bool {
    assert(amount > 0u64);
    assert(amount < 1000000u64);
    return true;
}

// Simulates private betting logic
transition place_bet(amount: u64) -> u64 {
    assert(amount > 0u64);
    assert(amount < 1000000u64);

    // Example logic: double the bet (simulated private computation)
    let result: u64 = amount * 2u64;

    return result;
}

// Simulated reward calculation
transition calculate_reward(amount: u64) -> u64 {
    assert(amount > 10u64);

    let reward: u64 = amount + 5u64;

    return reward;
}
```

}

Make sure syntax is correct and clean.

---

## 🔥 TASK 2 — UPDATE FRONTEND (SHOW REAL INTERACTION)

Update frontend to:

* Add input for bet amount
* Add buttons:

  * "Validate Bet"
  * "Place Bet"
* Simulate calling:

  * validate_bet
  * place_bet

Add:

const PROGRAM_ID = "private_claw.aleo";

Display results in UI:

* validation result
* calculated output

---

## 🔥 TASK 3 — ADD DEPLOYMENT PROOF (IMPORTANT)

Create a section in README called:

## Aleo Deployment Proof

Explain clearly:

* This project includes a fully functional Leo smart contract
* The contract is designed for Aleo Testnet deployment
* The frontend integrates with Shield Wallet for transaction execution

Add example:

Program ID: private_claw.aleo
Network: Aleo Testnet
Execution: via Shield Wallet

---

## 🔥 TASK 4 — ADD INTERACTION FLOW (JUDGE FRIENDLY)

Add to README:

## How It Works

1. User connects Shield Wallet
2. User enters bet amount
3. Frontend calls Leo contract functions
4. Logic executes privately (simulated)
5. Result is displayed

---

## 🔥 TASK 5 — IMPROVE PROJECT QUALITY

* Clean code
* Add comments in Leo contract
* Improve UI labels (clear buttons)
* Ensure everything looks real and usable

---

## ⚠️ RULES

* DO NOT use Aleo CLI
* DO NOT remove existing UI
* DO NOT simplify logic
* MUST look like a real privacy app

---

## OUTPUT

Show:

1. Updated Leo contract
2. Frontend changes
3. Final README
4. Project structure

Make this project clearly pass Buildathon judging.
